# ROI Legal App Dev Notes
- Node.js dependencies are configured under legalytics-react workspace.
- Mobile client builds are in the roi_app directory (Flutter).
- Verify environment parameters for legalytics API tests.
